---
import { LoginForm } from '@components/auth/LoginForm';
import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout title="Sign In" description="Sign in with Google to access your private area.">
  <div class="mx-auto flex min-h-[calc(100vh-200px)] max-w-md items-center px-4 sm:px-6">
    <div class="w-full rounded-2xl p-8 themed-card">
      <div class="mb-8 text-center">
        <h1 class="font-display text-3xl font-bold tracking-tight">
          Sign <span class="gradient-text">In</span>
        </h1>
        <p class="mt-2 text-sm" style="color: var(--color-text-secondary);">
          Access your private area with your Google account
        </p>
      </div>
      <LoginForm client:load />
    </div>
  </div>
</BaseLayout>
