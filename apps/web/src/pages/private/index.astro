---
import { PrivateDashboard } from '@components/private/PrivateDashboard';
import { PrivateLayout } from '@components/private/PrivateLayout';
import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout title="Area Privada" description="Tu area privada con herramientas y apps exclusivas.">
  <div class="pt-20">
    <PrivateLayout client:only="react" currentPath="/private">
      <PrivateDashboard client:only="react" />
    </PrivateLayout>
  </div>
</BaseLayout>
